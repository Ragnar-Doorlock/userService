const idError = {
    required: 'id field is required',
    tooShort: "ID's pipa is too short"
}

const nameError = {
    required: 'name is required'
}

const roleError = {
    required: 'Role field is required',
    invalid: 'Role is invalid'
}

module.exports = {idError, nameError, roleError};